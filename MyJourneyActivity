package com.SIMATS.rise;

import android.content.Intent;
import android.os.Bundle;
import android.widget.Button;
import android.widget.LinearLayout;
import android.widget.TextView;
import android.view.View;

import androidx.appcompat.app.AppCompatActivity;

public class MyJourneyActivity extends AppCompatActivity {

    Button backButton;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_my_journey);

        // Back button setup
        backButton = findViewById(R.id.backButton);
        backButton.setOnClickListener(v -> {
            Intent intent = new Intent(MyJourneyActivity.this, DashboardActivity.class);
            startActivity(intent);
            finish();
        });

        // Navigate to Planned Tasks
        TextView taskPlannedLabel = findViewById(R.id.taskPlannedLabel);
        taskPlannedLabel.setOnClickListener(v -> {
            Intent intent = new Intent(MyJourneyActivity.this, PlannedTasksActivity.class);
            startActivity(intent);
        });

        // Navigate to In Progress Tasks
        TextView taskInProgressLabel = findViewById(R.id.taskInProgressLabel);
        taskInProgressLabel.setOnClickListener(v -> {
            Intent intent = new Intent(MyJourneyActivity.this, InProgressTasksActivity.class);
            startActivity(intent);
        });

        // Navigate to Completed Tasks
        LinearLayout completedTasksLabel = findViewById(R.id.completedTaskSection);
        completedTasksLabel.setOnClickListener(v -> {
            Intent intent = new Intent(MyJourneyActivity.this, CompletedTasksActivity.class);
            startActivity(intent);
        });
    }
    // 👇 This must be outside onCreate()
    public void goToDashboard(View view) {
        Intent intent = new Intent(this, DashboardActivity.class);
        startActivity(intent);
        finish();
    }
}
